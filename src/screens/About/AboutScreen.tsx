import React from "react";
import styled from "styled-components";

const AboutScreen = () => {
  return <Root>Aboout Screen</Root>;
};

const Root = styled.div`
  flex: 1;
`;

const Title = styled.text`
  font-size: 20px;
  color: red;
`;

export default AboutScreen;
